@page
@model Clinica.Pages_Paciente.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Detalhes</h1>

<div>
    <h4>Paciente</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.tPaciente.PACIENTENOME)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.tPaciente.PACIENTENOME)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.tPaciente?.IDPACIENTE">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
