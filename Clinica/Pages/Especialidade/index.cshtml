@page
@model Clinica.Pages_Especialidade.indexModel

@{
    ViewData["Title"] = "index";
}

<h1>index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.tEspecialidade[0].NomeEspecialidade)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.tEspecialidade) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeEspecialidade)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.idEspecialidade">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.idEspecialidade">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.idEspecialidade">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
