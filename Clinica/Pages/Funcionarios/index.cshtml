@page
@model Clinica.Pages_Funcionarios.indexModel

@{
    ViewData["Title"] = "index";
}

<h1>index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.tFuncionarios[0].tFuncnome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.tFuncionarios) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.tFuncnome)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IdFuncionarios">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.IdFuncionarios">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.IdFuncionarios">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
